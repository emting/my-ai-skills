{
  "id": "lovable_github_cloudflare_worker",
  "name": "Lovable GitHub Cloudflare Worker",
  "version": "1.0.0",
  "description": "Stabilize a Lovable-generated project into a GitHub-managed and Cloudflare Worker-ready delivery pipeline.",
  "entrypoint": "SKILL.md",
  "runtime": "instruction_only",
  "inputs": {
    "project_path": {
      "type": "string",
      "required": true,
      "description": "Local path to the Lovable-generated project."
    },
    "github_repo": {
      "type": "string",
      "required": false,
      "description": "Target GitHub repository URL or identifier."
    },
    "cloudflare_target": {
      "type": "text",
      "required": false,
      "description": "Worker name, routes, bindings, and deployment constraints."
    }
  },
  "outputs": {
    "delivery_report": {
      "type": "markdown"
    }
  },
  "permissions": {
    "filesystem_read": true,
    "filesystem_write": true,
    "network": true
  },
  "safety": {
    "handles_sensitive_data": true,
    "requires_user_confirmation": true,
    "destructive": false,
    "rules": [
      "Do not print, commit, or transmit secrets, tokens, private keys, or account data.",
      "Require explicit user confirmation before git push, Cloudflare deploy, route/binding changes, or cloud resource changes.",
      "Run build, test, smoke test, and rollback checks before any deployment recommendation.",
      "Do not claim a deployment succeeded without observing an explicit provider result."
    ],
    "stop_conditions": [
      "授權、資料來源、範圍、身份或外部操作權限無法確認時停止高影響部分。",
      "發現來源矛盾、敏感資料暴露、輸出無法驗證或任務目標漂移時停止並要求人工判斷。"
    ],
    "approval_scope": [
      "exact_action",
      "exact_target",
      "exact_diff",
      "rollback_or_recovery_plan"
    ],
    "audit_requirements": [
      "record_sources",
      "record_assumptions",
      "record_approval_before_write"
    ],
    "rollback_required": true,
    "dry_run_default": true,
    "data_minimization": "redact_by_default"
  },
  "risk_level": "high",
  "source": {
    "local_path": "~/.skills/lovable-github-cloudflare-worker/SKILL.md",
    "status": "imported_local"
  },
  "schema_version": "1.1.0",
  "contract_version": "1.1.0",
  "capabilities": {
    "filesystem": "workspace_read_write",
    "network": "public_read_only",
    "shell": "none",
    "git": "none",
    "browser": "none"
  },
  "connectors": [],
  "data_egress": {
    "mode": "public_data_only",
    "connectors": [],
    "allowed_data_classes": [
      "public_data"
    ],
    "approval_required": true,
    "minimize_and_redact": true,
    "retention": "none_by_default"
  },
  "external_write": {
    "allowed": false,
    "mode": "draft_or_read_only",
    "approval_required": true,
    "approval_scope": []
  },
  "execution": {
    "executor": "instruction_only",
    "adapter": "SKILL.md",
    "network_is_not_implied": true
  },
  "activation": {
    "positive_examples": [
      "使用者明確要求：Stabilize a Lovable-generated project into a GitHub-managed and Cloudflare Worker-ready delivery pipeline."
    ],
    "negative_examples": [
      "任務只涉及與「Lovable GitHub Cloudflare Worker」無關的主題，不需要其專門流程。"
    ],
    "exclude_when": [
      "若任務不涉及「Lovable GitHub Cloudflare Worker」的核心目的、輸入或輸出，不要啟用本技能。"
    ],
    "priority": 80,
    "delegates_to": [],
    "selection_notes": "先檢查任務目的、輸入資料與外部權限；相鄰技能重疊時以最小權限與最窄範圍優先。"
  },
  "related_skills": []
}
